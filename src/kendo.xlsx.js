(function(f, define){
    define([ "./kendo.core" ], f);
})(function(){

var __meta__ = {
    id: "xlsx",
    name: "XLSX export",
    category: "framework",
    advanced: true,
    depends: [ "core" ]
};

(function(JSZip){

})(JSZip);

return window.kendo;

}, typeof define == 'function' && define.amd ? define : function(_, f){ f(); });
