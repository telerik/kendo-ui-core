@(Html.Kendo().Scheduler<Kendo.Mvc.Examples.Models.Scheduler.Task>()
    .Name("Scheduler")
    .Events(e => e.Edit("onEdit"))
    .Resources(r =>
    {
        r.Add(m => m.OwnerID)
            .Title("Owner")
            .Multiple(true)
            .DataTextField("Text")
            .DataValueField("Value")
            //.DataSource(d => d.Read("Attendies", "Scheduler"));
            .BindTo(new[] { 
                new { Text = "Alex", Value = 1, color = "red" } ,
                new { Text = "Bob", Value = 1, color = "green" } ,
                new { Text = "Charlie", Value = 1, color = "blue" } 
            });    
    })
    //.BindTo(new List<Kendo.Mvc.Examples.Models.Scheduler.Task>() { 
    //    new Kendo.Mvc.Examples.Models.Scheduler.Task() {
    //        TaskID = 1, 
    //        Title = "Business meeting",
    //        Description = "Business meeting with colegue.",
    //        Start = new DateTime(2013,6,12),
    //        End = new DateTime(2013,6,13)   
    //    } 
    //})
    .DataSource(d => d
        .Model(m => m.Id(f => f.TaskID))
           .Read("Read", "Scheduler")
           .Create("Create", "Scheduler")
           .Destroy("Destroy", "Scheduler")
           .Update("Update", "Scheduler")

    )
)

<script type="text/javascript">
    function onEdit(e) {
        console.log("Edit event fired");
    }
</script>