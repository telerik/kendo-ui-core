<div class="demo-section">
    @(Html.Kendo().TreeView()
        .Name("treeview")
        .DataTextField("Name")
        .DataSource(dataSource => dataSource
            .Read(read => read
                .Action("Employees", "TreeView")
            )
        )
        .CheckboxTemplate("# var name = 'checkedFiles[' + item.id + ']'; #<input type='checkbox' name='#= name #' value='true' />")
    )
</div>

<style scoped>
    .demo-section {
        width: 200px;
    }
</style>