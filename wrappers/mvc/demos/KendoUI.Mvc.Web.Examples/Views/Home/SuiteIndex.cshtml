<!DOCTYPE html>
<html>
<head>
    <title>Kendo UI Web Examples</title>
    <link href="@Url.Content("~/Content/suite.css")" rel="stylesheet" type="text/css" />
</head>
<body>
    <div id="page">
        @foreach (string category in ViewBag.Navigation.Keys)
        {
            <h1>@category</h1>
            <ul>
                @foreach (NavigationWidget widget in ViewBag.Navigation[category])
                {
                    if (widget.OnlineOnly) { continue; }
                    <li>
                        <h2>@widget.Text</h2>
                        <ul>
                            @foreach (NavigationExample example in widget.Items)
                            {
                                <li><a href='@example.Url'>@example.Text</a></li>
                            }
                        </ul>
                    </li>
                }
            </ul>
        }
    </div>
</body>
</html>
