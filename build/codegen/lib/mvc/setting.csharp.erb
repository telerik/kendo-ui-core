namespace Kendo.Mvc.UI
{
    using System;
    using System.Linq;
    using System.Collections.Generic;
    using System.Web.Routing;
    using Kendo.Mvc.Extensions;

    public class <%= self.instance_of?(array_option_class) ? csharp_item_class : csharp_class %> : JsonObject
    {
        public <%= self.instance_of?(array_option_class) ? csharp_item_class : csharp_class %>()
        {<% if toggleable %>
            Enabled = <%= default %>;
        <% end %>
            //>> Initialization
            //<< Initialization
        }<% if toggleable %>

        public bool Enabled { get; set; }<% end %>

        //>> Fields
        //<< Fields

        protected override void Serialize(IDictionary<string, object> json)
        {
            //>> Serialization
            //<< Serialization
        }
    }
}
