<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <meta charset="utf-8">
    <link href="../../content/shared/styles/examples-offline.css" rel="stylesheet">
    <link href="../../../styles/kendo.common.min.css" rel="stylesheet">
    <link href="../../../styles/kendo.rtl.min.css" rel="stylesheet">
    <link href="../../../styles/kendo.default.min.css" rel="stylesheet">

    <script src="../../../js/jquery.min.js"></script>
    <script src="../../../js/kendo.web.min.js"></script>
    <script src="../../content/shared/js/console.js"></script>
    <script>
        <%= options[:additional_code] %>
    </script>
    <% options[:additional_scripts].each do |script| %><script src="<%= script %>"></script><% end %>
    <% options[:additional_stylesheets].each do |stylesheet| %><link href="<%= stylesheet %>" rel="stylesheet"><% end %>
</head>
<body>
    <% unless options[:skip_back_button] %>
        <a class="offline-button" href="../index.html">Back</a>
    <% end %>
    <%= body %>
	
	<% if requiresServer %>
		<div class='demo-section'>
			<h3>Note:</h3>
			<p>
				Security restrictions prevent this example from working in all browsers when the page is loaded from the file system (via file:// protocol). 
				If the demo is not working as expected, please host the Kendo folder on a web server. 
			</p>
		</div>
	<% end %>		
</body>
</html>
