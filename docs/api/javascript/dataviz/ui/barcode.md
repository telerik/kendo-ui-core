---
title: Barcode
page_title: Configuration, methods and events of Kendo UI DataViz Barcode
description: Manipulate the configuration options of Barcode, configure the color of the bars/text and change the value.
res_type: api
component: barcode
---

# kendo.dataviz.ui.Barcode

## Configuration

### renderAs `String` *(default: "svg")*

Sets the preferred rendering engine.
If it is not supported by the browser, the Barcode will switch to the first available mode.

The supported values are:

* "canvas" - renders the widget as a Canvas element, if available.
* "svg" - renders the widget as inline SVG document, if available


<div class="meta-api-description">
How does the renderAs property in Kendo UI's barcode component affect the appearance of generated barcodes? Configure how the barcode is visually generated by selecting the rendering method to either draw using Canvas or as inline SVG graphics, enabling control over output format and compatibility with different browsers or environments. Choose between rendering strategies such as Canvas pixels or scalable vector graphics to optimize appearance, performance, or integration requirements, with automatic fallback to supported renderers if the preferred mode is unavailable. This setting controls whether barcodes appear as bitmap-based Canvas drawings or resolution-independent SVG shapes, useful for adapting to display preferences, export formats, or customization needs in barcode generation workflows.
</div>

#### Example - Render as SVG, if supported

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      width: 300,
      value:"123456",
      renderAs: "svg"
    });
    </script>

### background `String` *(default: "white")*

The background of the barcode area.
Any valid CSS color string will work here, including hex and rgb.


<div class="meta-api-description">
How do I change the background color of a Kendo UI for jQuery barcode? Adjust or configure the background color behind a barcode using any standard CSS color format including hex codes, RGB, RGBA, HSL, or color names to customize the barcode’s visual area, control the appearance of the barcode’s backdrop, set or change the barcode panel’s background hue, and manage styling related to the barcode container for contrast or branding purposes during rendering or initialization.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      width: 300,
      value: "HELLO WORLD",
      background: "#2eb3a6"
    });
    </script>

### border `Object`

The border of the barcode area.


<div class="meta-api-description">
How do I change the border style of a barcode in Kendo UI? Customize and control the visible outline around barcode elements by setting border color, thickness, dash styles, stroke patterns, and edge appearance to enhance barcode visibility, style, or separation from surrounding content. Adjust border rendering options to define barcode edge aesthetics, apply solid or dashed lines, modify width and color for clear delineation or decorative effects, and configure border styling to meet design or scanning requirements when generating or displaying barcodes.
</div>

#### Example - set the border of the barcode

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value:"123456",
      width: 300,
      border: {
        width: 2,
        dashType: "solid",
        color: "black"
      }
    });
    </script>

### border.color `String` *(default: "black")*

The color of the border. Any valid CSS color string will work here, including hex and rgb.


<div class="meta-api-description">
How to change the border color of a Kendo UI for jQuery Barcode component? Control and customize the outline color of barcode displays by configuring the edge hue with any valid CSS color format such as hexadecimal codes, RGB, RGBA, HSL values, or standard named colors to style borders, frames, and boundary lines around barcode visuals for enhanced appearance, theme matching, and clear separation from backgrounds or adjacent elements.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
        width: 300,
        value: "123456",
        border: {
            color: "red"
        }
    });
    </script>

### border.dashType `String` *(default: "solid")*

The dash type of the border.


<div class="meta-api-description">
How to customize the border style of a Kendo UI Barcode widget? Configure the border stroke pattern of barcode visuals by setting the line style to solid, dashed, or dotted, enabling control over the border’s appearance with custom dash formats and stroke styles; ideal for customizing barcode outlines, adjusting border rendering, or toggling between continuous and segmented border lines for visual emphasis or design consistency.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
        width: 300,
        value: "123456",
        border: {
            dashType: "dash"
        }
    });
    </script>

### border.width `Number` *(default: 0)*

The width of the border.


<div class="meta-api-description">
How do I adjust the thickness of the outline around a Kendo UI barcode? Adjust the thickness, size, or weight of the outline or frame around a barcode to control its visibility, appearance, and spacing; customize, set, or configure the border width to make the barcode edges thicker or thinner for better emphasis, clarity, or design consistency in barcode rendering and display.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
        width: 300,
        value: "123456",
        border: {
            width: 2
        }
    });
    </script>

### checksum `Boolean` *(default: false)*

If set to `true`, the Barcode will display the checksum digit next to the value in the text area.


<div class="meta-api-description">
How do I display the checksum digit in a Kendo UI barcode? Enable or disable displaying the calculated checksum digit alongside the barcode value for error detection, validation, verification, or human-readable confirmation purposes; configure whether to show the checksum character next to the barcode text to assist in manual scanning accuracy, debugging barcode data integrity, or verifying encoded information, with options to toggle checksum visibility on or off in barcode display components.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      type: "ean8",
      value: "1234567",
      checksum: true
    });
    </script>

### color `String` *(default: "black")*

The color of the bar elements.
Any valid CSS color string will work here, including hex and rgb.


<div class="meta-api-description">
How do I change the color of barcode bars in Kendo UI for jQuery? Change or customize the color of barcode bars by specifying any valid CSS color format such as hex codes, RGB, RGBA, HSL, or named colors, enabling control over the barcode’s fill appearance to match design requirements, themes, or branding. Adjust, set, or define bar fill colors to enhance barcode visibility, contrast, or style in user interfaces, print layouts, or digital displays. Whether configuring color through hexadecimal values, color functions, or common color names, this feature supports flexible visual customization of barcode elements for optimal aesthetic or functional integration.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      type: "ean13",
      value: "123456789987",
      color: "#10c4b2"
    });
    </script>

### height `Number` *(default: 100)*

The height of the barcode in pixels.  By default the height is 100.


<div class="meta-api-description">
How to adjust the height of a Kendo UI barcode control? Adjust or configure the vertical dimension, pixel height, or overall size of a barcode for display or print output, enabling control over how tall the barcode appears on screen or physical media. This includes setting barcode image height, scaling barcode height, resizing barcode vertically, customizing barcode height in pixels, managing barcode dimensions for visibility, and controlling barcode print height for clarity. Whether you want to increase, decrease, set, or fine-tune barcode height for better scanning, formatting, or presentation purposes, this adjustment handles various scenarios related to barcode vertical sizing and layout.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      type: "ean13",
      value: "123456789987",
      color: "#10c4b2",
      height: 300
    });
    </script>

### padding `Object`

The padding of the barcode.


<div class="meta-api-description">
How to adjust the spacing around a barcode graphic in Kendo UI for jQuery? Adjust, configure, or set the inner spacing or whitespace around barcode graphics to control the distance between the barcode content and its outer boundaries, enabling precise alignment, balanced visual layout, optimized printing margins, and proper spacing within surrounding design elements or user interfaces. This spacing control enhances barcode placement, prevents clipping or crowding, and allows fine-tuning of padding or margins to improve readability and integration within various applications or layouts.
</div>

#### Example - set the padding of the barcode

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value:"123456",
      width: 300,
      padding: {
        top: 20,
        left: 5,
        right: 5,
        bottom: 5
      }
    });
    </script>

### padding.bottom `Number` *(default: 0)*

The bottom padding of the barcode.


<div class="meta-api-description">
How do I adjust the padding at the bottom of a barcode in Kendo UI for jQuery? Adjust vertical spacing or set the bottom margin beneath a barcode to control the empty space below it, configure the lower padding area around barcode content for precise layout, customize how much whitespace appears under the barcode graphic, modify bottom inset or buffer zone to separate the barcode from adjacent elements, enable control over vertical padding on the barcode's underside during initialization, set or tweak bottom spacing for barcode alignment and positioning within a container, manage the gap below the barcode to ensure proper visual separation, control how much bottom room appears under barcode data, configure vertical offset or lower boundary padding to influence barcode placement and spacing in user interfaces.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
        width: 300,
        value: "123456",
        padding: {
            bottom: 20
        }
    });
    </script>

### padding.left `Number` *(default: 0)*

The left padding of the barcode.


<div class="meta-api-description">
How do I adjust the left margin in Kendo UI for jQuery Barcode component? Adjust or set the left-side margin, space, or gap between barcode content and its container edge to control horizontal offset, left padding, or inner spacing on the barcode's left boundary. Enable precise alignment, customize spacing, shift barcode positioning inward from the left border, or configure left indent within barcode layout settings for better visual balance and fit. Manage left inner margin, left inset, or padding space to ensure barcode elements are not flush against the edge, supporting layout fine-tuning and responsive design.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
        width: 300,
        value: "123456",
        padding: {
            left: 15
        }
    });
    </script>

### padding.right `Number` *(default: 0)*

The right padding of the barcode.


<div class="meta-api-description">
How do I adjust the right margin of a Kendo UI barcode control? Control or configure the horizontal right-side spacing or margin around a barcode image to adjust empty space between the barcode and its outer container or surrounding elements, enabling precise alignment, layout tuning, margin adjustments for printing, avoiding overlap with other UI components, and fine-tuning the distance on the barcode’s right edge to prevent clipping or collisions in various display or print scenarios.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
        width: 300,
        value: "123456",
        padding: {
            right: 25
        }
    });
    </script>

### padding.top `Number` *(default: 0)*

The top padding of the barcode.


<div class="meta-api-description">
How to adjust the spacing above the barcode graphics in Kendo UI for jQuery? Adjust and control the space above barcode graphics by configuring top margin or padding to shift barcode positioning downward for better alignment, layout adjustments, print formatting, or spacing within containers and surrounding components. Enable setting upper spacing to manage white space above barcode elements, customize vertical offset, and fine-tune barcode placement within user interfaces, print layouts, or design compositions to achieve consistent alignment and appropriate margins.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
        width: 300,
        value: "123456",
        padding: {
            top: 30
        }
    });
    </script>

### text `Object`

Can be set to a JavaScript object which represents the text configuration.


<div class="meta-api-description">
How do I customize the human-readable label displayed with a Kendo UI for jQuery Barcode component? Configure and customize the human-readable label or caption displayed with a barcode, including controlling its content, style, formatting, visibility, font, alignment, position relative to the barcode, and text appearance. Adjust or set the accompanying barcode text to show specific data, format the text output, enable or disable visibility, reposition the text above, below, or beside the barcode, and fine-tune font size, style, or color to match design requirements. This lets you manage how barcode information is presented to users, for clearer identification, improved readability, or branding purposes.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value:"123456",
      width: 300,
      text:{
        color: "red",
        font: "20px sans-serif"
      }
    });
    </script>

### text.color `String` *(default: "black")*

The color of the text. Any valid CSS color string will work here, including hex and rgb.


<div class="meta-api-description">
How do I change the color of barcode text in Kendo UI? Adjust or define the color for barcode text rendering to enhance visibility, readability, or aesthetic styling by specifying any standard CSS color format such as hex codes, RGB, RGBA, HSL, or named colors; control or customize text appearance on barcodes to match themes, branding, user interface design, and accessibility requirements through flexible color configuration during component setup or runtime styling.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
        width: 300,
        value: "123456",
        text: {
            color: "blue"
        }
    });
    </script>

### text.font `String` *(default: "16px Consolas, Monaco, Sans Mono, monospace, sans-serif")*

The font of the text.


<div class="meta-api-description">
How do I change the font style of the human-readable text in a Kendo UI barcode component? Set, customize, or control the typography, font family, size, weight, and style for readable text labels displayed alongside or within barcode renderings, enabling styling of human-readable text elements in barcode components, including configuring font appearance, typeface selection, font scaling, bold or italic formatting, and text rendering options for barcode annotations or captions during initialization or runtime.
</div>

#### Example - set the font of the text

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value:"123456",
      width: 300,
      text:{
        color: "red",
        font: "20px sans-serif"
      }
    });
    </script>

### text.margin `Object`

The margin of the text


<div class="meta-api-description">
How do I adjust the margin around the text in a Kendo UI barcode widget? Adjust, configure, or control the spacing, padding, or margin around the barcode label text to fine-tune its position, prevent overlap, clipping, or crowding, and customize the offset or white space between the barcode graphics and its associated text during rendering or initialization, ensuring clear visibility and optimal layout of barcode captions or annotations.
</div>

#### Example - set the margin of the text.

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value:"123456",
      width: 300,
      text:{
        margin : {
          top: 3
        }
      }
    });
    </script>

### text.margin.bottom `Number` *(default: 0)*

The bottom margin of the text.


<div class="meta-api-description">
How do I adjust the space between the barcode value and its label in Kendo UI for jQuery? Control and adjust the vertical spacing or padding below the barcode label, allowing customization of the whitespace beneath the displayed barcode text, set or configure the bottom margin to increase or decrease space under the barcode value, manage text margin or offset for optimal readability, fine-tune the gap or clearance between the barcode graphic and its associated text, customize the lower text padding for layout alignment, modify spacing to prevent overlap or improve visual separation under the barcode’s text label.
</div>

#### Example - apply a bottom margin

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value:"123456",
      width: 300,
      text:{
        margin: {
        	bottom: 20
      	}
      }
    });
    </script>

### text.margin.left `Number` *(default: 0)*

The left margin of the text.


<div class="meta-api-description">
How do I adjust the left margin of the barcode text in Kendo UI? Adjust, set, or control the horizontal left spacing or padding for the text label displayed near a barcode, allowing precise alignment, offset, or positioning of the human-readable barcode characters by increasing or decreasing left margin or left indent of the barcode text to customize how the label sits relative to the barcode image, useful for fine-tuning text placement, improving readability, or matching design layout requirements.
</div>

#### Example - apply a left margin

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value:"123456",
      width: 300,
      text:{
        margin: {
        	left: 20
      	}
      }
    });
    </script>

### text.margin.right `Number` *(default: 0)*

The right margin of the text.


<div class="meta-api-description">
How do I adjust the spacing between the barcode value and the right margin in Kendo UI for jQuery? Adjust the right-side spacing or padding of barcode label text to control text offset, horizontal alignment, and spacing between the barcode value and the right margin; set, configure, or customize the right margin of barcode text to fine-tune label positioning, text placement, and alignment in barcode rendering layouts or printing.
</div>

#### Example - apply a right margin

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value:"123456",
      width: 300,
      text:{
        margin: {
        	right: 20
      	}
      }
    });
    </script>

### text.margin.top `Number` *(default: 0)*

The top margin of the text.


<div class="meta-api-description">
How do I adjust the space between a Kendo UI barcode and its text label? Adjust the vertical spacing, offset, or padding above the text label in a barcode display to control the layout and positioning of the barcode’s text element relative to the barcode graphic. This setting enables configuration of the top margin, top padding, or distance between the barcode and its descriptive text, allowing customization of text placement and alignment above the barcode for improved readability and visual balance. Developers often seek to set, increase, decrease, or fine-tune the gap or white space above barcode labels, text captions, or annotations for user interfaces, printing layouts, or scanning applications.
</div>

#### Example - apply a top margin

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value:"123456",
      width: 300,
      text:{
        margin: {
        	top: 20
      	}
      }
    });
    </script>

### text.visible `Boolean` *(default:true)*

If set to false the barcode will not display the value as a text below the barcode lines.


<div class="meta-api-description">
How can I hide the human-readable code value underneath a Kendo UI barcode? Enable or disable the display of the human-readable code value underneath barcode lines, control whether the barcode’s textual representation is shown or hidden, configure visibility of the number or string below barcode bars, toggle showing the readable text below the barcode image, set the option to render or suppress the barcode’s decoded text for scanning and user verification, manage if the barcode label or value is visible beneath the graphic, adjust the setting to show or hide the characters representing the encoded data under the barcode, customize the presentation to include or exclude the barcode’s associated text, and determine whether the barcode’s plaintext content is displayed for clarity or concealed for cleaner appearance.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value:"123456",
      text:{
        visible: false
      },
      height: 300
    });
    </script>

### type `String` *(default: "code39")*

The symbology (encoding) the barcode will use.

The supported values are:

* EAN8
* EAN13
* UPCE
* UPCA
* Code11
* Code39
* Code39Extended
* Code93
* Code93Extended
* Code128
* Code128A
* Code128B
* Code128C
* GS1-128
* MSImod10
* MSImod11
* MSImod1010
* MSImod1110
* POSTNET


<div class="meta-api-description">
How do I set the type for EAN13 barcodes using Kendo UI for jQuery? Select or configure the barcode encoding, symbology, or format standard to generate barcodes such as EAN, UPC, Code39, Code128, GS1 variants, MSI options, or POSTNET for flexible barcode rendering. Enable setting barcode types including EAN8, EAN13, UPCE, UPCA, Code11, Code39 extended forms, Code93 variants, multiple Code128 subsets (A, B, C), GS1-128 specification, and MSI with mod10, mod11, mod1010, mod1110 checksum control for barcode generation or scanning use cases. Control barcode standards, encoding formats, and symbologies for customized barcode output compatible with retail, inventory, shipping, or logistics scenarios, allowing developers to specify desired barcode types programmatically.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      type: "code128",
      value:"Hello World",
      width: 400
    });
    </script>

### value `String`

The initial value of the Barcode


<div class="meta-api-description">
How do I set the initial value of a Kendo UI barcode? Configure or set the initial data, text, or payload that a barcode displays or encodes when first created, controlling the barcode’s starting value, content, or string; specify the encoded information, default text, or initial input for barcode generation, allowing developers to define what code or data the barcode represents at initialization, including embedding, setting, or providing the barcode’s encoded content or the text to be transformed into the scannable pattern.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value:"12345",
      width: 300
    });
    </script>

### width `Number` *(default: 300)*

The width of the barcode in pixels.  By default the width is 300.


<div class="meta-api-description">
How do I set the width of a barcode in Kendo UI for jQuery? Adjust or set the horizontal size, width, or pixel dimensions of barcodes for rendering, printing, resizing, or layout purposes; configure the barcode's visual width in pixels to control how wide the barcode appears on screen or in printed output, enabling developers to define, change, or customize barcode width dynamically or during initialization to fit design requirements, display constraints, or device resolutions.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      type: "code128",
      value:"Hi",
      width: 200
    });
    </script>

## Methods

### exportImage
Exports the barcode as an image.
The result can be saved using [kendo.saveAs](/api/javascript/kendo/methods/saveas).

The export operation is asynchronous and returns a [promise](https://api.jquery.com/Types/#Promise).
The promise will be resolved with a PNG image encoded as a [Data URI](https://developer.mozilla.org/en-US/docs/data_URIs).


<div class="meta-api-description">
How to convert Kendo UI barcode component to high-quality image in PNG format? Export barcode rendering as a high-quality PNG image for saving, downloading, or further manipulation by invoking a method that asynchronously produces a promise resolved with a base64-encoded Data URI of the barcode image, enabling easy integration with file saving utilities or custom image processing workflows, supporting use cases like exporting generated barcodes, converting barcode visuals to image files, or automating image capture from barcode components for storage and sharing.
</div>

#### Parameters

##### options `Object` *(optional)*
Parameters for the exported image.

##### options.width `String`
The width of the exported image. Defaults to the barcode width.

##### options.height `String`
The height of the exported image. Defaults to the barcode height.

#### Returns
`Promise` A promise that will be resolved with a PNG image encoded as a Data URI.

#### Example - Exporting a barcode to an image
    <div id="barcode"></div>
    <script>
        $("#barcode").kendoBarcode({
          value: "BAR",
          width: 300
        });

        var barcode = $("#barcode").getKendoBarcode();
        barcode.exportImage().done(function(data) {
            kendo.saveAs({
                dataURI: data,
                fileName: "barcode.png"
            });
        });
    </script>


### exportPDF
Exports the barcode as a PDF file.
The result can be saved using [kendo.saveAs](/api/javascript/kendo/methods/saveas).

The export operation is asynchronous and returns a [promise](https://api.jquery.com/Types/#Promise).
The promise will be resolved with a PDF file encoded as a [Data URI](https://developer.mozilla.org/en-US/docs/data_URIs).


<div class="meta-api-description">
How to generate a PDF file containing a barcode image using Kendo UI for jQuery? Generate and save or download a PDF file containing the barcode image on the client side, enabling export of barcodes as PDF documents for sharing, printing, or storage; supports asynchronous processing with promise-based handling, allows exporting barcode data to a PDF format encoded as a data URI, and integrates with file-saving utilities to facilitate client downloads or programmatic file saving of barcode scans or generated barcode visuals.
</div>

#### Parameters

##### options `kendo.drawing.PDFOptions` *(optional)*
Parameters for the exported PDF file.

#### Returns
`Promise` A promise that will be resolved with a PDF file encoded as a Data URI.

#### Example - Exporting a barcode to a PDF file
    <div id="barcode"></div>
    <script>
        $("#barcode").kendoBarcode({
          value: "BAR",
          width: 300
        });

        var barcode = $("#barcode").getKendoBarcode();
        barcode.exportPDF({ paperSize: "A5", landscape: true }).done(function(data) {
            kendo.saveAs({
                dataURI: data,
                fileName: "barcode.pdf"
            });
        });
    </script>


### exportSVG
Exports the barcode as an SVG document.
The result can be saved using [kendo.saveAs](/api/javascript/kendo/methods/saveas).

The export operation is asynchronous and returns a [promise](https://api.jquery.com/Types/#Promise).
The promise will be resolved with a SVG document encoded as a [Data URI](https://developer.mozilla.org/en-US/docs/data_URIs).


<div class="meta-api-description">
How can I export a barcode as an SVG file using Kendo UI for jQuery? Generate, save, download, or embed barcode images as scalable vector graphics by exporting rendered barcodes to SVG format, enabling high-quality printing, digital use, or integration in web and mobile applications; configure or trigger asynchronous export processes that return promises with SVG data URIs for seamless saving or manipulation, supporting workflows needing vector-based barcode output, export barcode visuals as standalone files or inline SVG documents, and handle barcode conversion to SVG for further customization, embedding, or sharing.
</div>

#### Parameters

##### options `Object` *(optional)*
Export options.

##### options.raw `Boolean` *(default: false)*
Resolves the promise with the raw SVG document without the Data URI prefix.

#### Returns
`Promise` A promise that will be resolved with a SVG document encoded as a Data URI.

#### Example - Exporting a barcode to an SVG document
    <div id="barcode"></div>
    <script>
        $("#barcode").kendoBarcode({
          value: "BAR",
          width: 300
        });

        var barcode = $("#barcode").getKendoBarcode();
        barcode.exportSVG().done(function(data) {
            kendo.saveAs({
                dataURI: data,
                fileName: "barcode.svg"
            });
        });
    </script>

### imageDataURL

Returns a PNG image of the barcode encoded as a [Data URL](https://developer.mozilla.org/en-US/docs/data_URIs).

> This method is deprecated and replaced by [exportImage](/api/javascript/dataviz/ui/barcode/methods/exportimage).


<div class="meta-api-description">
How do I get a base64-encoded PNG image string of a Kendo UI barcode for embedding in HTML? Generate or get a base64-encoded PNG image string of a barcode for embedding in HTML img tags, inline embedding, downloading, or uploading to servers, convert barcode visuals to data URLs for easy image handling, extract barcode graphics as encoded strings for front-end or back-end processing, enable exporting barcode images as inline data URIs usable in web apps or APIs, obtain barcode visuals in a portable, text-encoded image format suitable for embedding or transfer, encode barcode renderings into PNG data URLs for dynamic image source assignment or server-side workflows.
</div>

#### Returns

`String` A data URL with `image/png` MIME type. Will be `null` if the browser does not support the `canvas` element.

#### Example - show a snapshot of the Barcode

    <div id="barcode"></div>
    <a download="export.png" id="export" class="k-button">Export PNG</a>
    <script>
    $("#barcode").kendoBarcode({
      value: "FOO",
      width: 300
    });

    $("#export").on("click", function() {
      var barcode = $("#barcode").data("kendoBarcode");
      var imageDataURL = barcode.imageDataURL();

      if (navigator.msSaveBlob) {
        var blob = toBlob(imageDataURL, "image/png");
        navigator.msSaveBlob(blob, this.getAttribute("download"));
      } else {
        this.href = imageDataURL;
      }
    });

    // See: https://goo.gl/qlg5dd
    function toBlob(base64, type) {
      var rawData = base64.substring(base64.indexOf("base64,") + 7);
      var data = atob(rawData);
      var arr = new Uint8Array(data.length);

      for (var i = 0; i < data.length; ++i) {
        arr[i] = data.charCodeAt(i);
      }

      return new Blob([ arr.buffer ], { type: type });
    }
    </script>

### redraw

Redraws the barcode.


<div class="meta-api-description">
How do I manually refresh a Kendo UI for jQuery barcode after changing its configuration settings? Trigger or force the barcode display to update and refresh its visual rendering whenever data, configuration settings, style options, or container dimensions change; manually invoke a method to re-render the barcode dynamically after modifying values, layout, formatting, or appearance to ensure the barcode output accurately reflects the latest state, supporting use cases where automatic updates might not occur or when immediate visual refresh is needed following property adjustments or interactive changes.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value: "FOO",
      width: 300
    });

    $("#barcode")
       .data("kendoBarcode").redraw();
    </script>

### resize

Adjusts the widget layout to match the size of the container.


<div class="meta-api-description">
How do I update the Kendo UI barcode to resize automatically when its container changes size? Adjust or update the barcode display to fit new container dimensions by recalculating size, layout, and visuals whenever the surrounding layout or element size changes due to window resizing, dynamic visibility toggles, CSS modifications, or responsive design updates. Trigger recalculation of width and height, refresh the barcode rendering, and ensure it scales properly after programmatic size adjustments or DOM changes, enabling seamless resizing and layout adaptation without restarting or reinitializing the barcode component.
</div>

#### Example

    <div id="barcode" style="width: 300px; height: 150px;"></div>
    <script>
    $("#barcode").kendoBarcode({
      value: "FOO"
    });

    $("#barcode")
       .css("width", "600px")
       .data("kendoBarcode").resize();
    </script>

> **Important**
>
> If a fixed width is set using the [width](/api/javascript/dataviz/ui/barcode#configuration-width) option, the [resize](/api/javascript/dataviz/ui/barcode/methods/resize) method will not adjust the widget to match the size of the container. In this case, update both the container's width and the widget's width to the appropriate value.

#### Example

    <div id="barcode" style="width: 300px; height: 150px;"></div>
    <script>
    $("#barcode").kendoBarcode({
      value: "FOO",
      width: 300
    });

    $("#barcode").css("width", "600px");
    var barcode = $("#barcode").data("kendoBarcode");
    barcode.setOptions({ width: 600 });
    barcode.resize();
    </script>

#### Parameters

##### force `Boolean` *optional*

Defines whether the widget should proceed with resizing even if the element dimensions have not changed.

### svg

Returns the [SVG](https://www.w3.org/Graphics/SVG/) representation of the barcode. The returned string is a self-contained SVG document that can be used as is or converted to other formats using tools like [Inkscape](https://inkscape.org/en) and
[ImageMagick](https://www.imagemagick.org/). Both programs provide command-line interface suitable for server-side processing.

> This method is obsoleted by [exportSVG](/api/javascript/dataviz/ui/barcode/methods/exportsvg), but will remain fully functional.


<div class="meta-api-description">
How to export Kendo UI Barcode as an SVG string? Generate or retrieve a self-contained SVG string representing the barcode output for purposes such as exporting, saving, printing, or converting to other image formats; enable extraction of the barcode as a standalone SVG document suitable for use in server-side workflows, command-line tools, or graphic converters like Inkscape and ImageMagick, providing a flexible vector format export method that supports automated processing, embedding, or further manipulation in various environments.
</div>

#### Returns

`String` the SVG representation of the barcode.

#### Example - get the SVG representation of the barcode

    <div id="barcode"></div>
    <script>
    $("#barcode").kendoBarcode({
      value: "FOO",
      width: 300
    });
    var barcode = $("#barcode").data("kendoBarcode");
    var svg = barcode.svg();
	/* The result can be observed in the DevTools(F12) console of the browser. */
    console.log(svg); // displays the SVG string
    </script>

### value

Gets/Sets the value of the barcode.


<div class="meta-api-description">
How do I get the current barcode value in Kendo UI? Retrieve, read, access, get, or fetch the current barcode data or content programmatically, as well as update, set, change, modify, or overwrite the barcode value dynamically within the barcode component by using a method that returns the existing barcode value when called without parameters or updates the displayed barcode when called with a new input, supporting scenarios where developers need to control, configure, manipulate, or manage barcode information in applications, including resetting, refreshing, or syncing barcode content through code.
</div>

#### Example

    <div id="barcode"></div>
    <script>
    $('#barcode').kendoBarcode({
        width: 300,
        value: "123456"
    });
    // get a reference to the barcode widget
    var barcode = $("#barcode").data("kendoBarcode");

    // get the value of the barcode.
    var value = barcode.value();
	/* The result can be observed in the DevTools(F12) console of the browser. */
    console.log(value);

    // sets the value of the barcode and redraws it.
    barcode.value("1234567");
    </script>

#### Parameters

##### value `Number | String`

The value to set.

#### Returns

`String` The value of the barcode.
