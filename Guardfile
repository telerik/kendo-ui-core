# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload', :apply_js_live => false do
  watch(%r{src/.*})
  watch(%r{styles/.*})
  watch(%r{tests/.*})
  watch(%r{playground/.*})
end

